<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <title>Editar Endereço</title>
  </head>
  <body>
    <h1>Editar Endereço</h1>
    <% if (typeof error !== 'undefined') { %>
    <p style="color: red"><%= error %></p>
    <% } %>
    <form action="/enderecos/editar/<%= endereco.id %>" method="POST">
      <div>
        <label for="rua">Rua:</label>
        <input type="text" id="rua" name="rua" value="<%= endereco.rua %>" required />
      </div>
      <div>
        <label for="numero">Número:</label>
        <input type="text" id="numero" name="numero" value="<%= endereco.numero || '' %>" />
      </div>
      <div>
        <label for="bairro">Bairro:</label>
        <input type="text" id="bairro" name="bairro" value="<%= endereco.bairro %>" required />
      </div>
      <div>
        <label for="cidade">Cidade:</label>
        <input type="text" id="cidade" name="cidade" value="<%= endereco.cidade %>" required />
      </div>
      <div>
        <label for="estado">Estado (2 caracteres):</label>
        <input type="text" id="estado" name="estado" value="<%= endereco.estado %>" maxlength="2" required />
      </div>
      <div>
        <label for="cep">CEP (9 caracteres):</label>
        <input type="text" id="cep" name="cep" value="<%= endereco.cep %>" maxlength="9" required />
      </div>
      <button type="submit">Atualizar</button>
    </form>
    <br />
    <a href="/enderecos">Voltar para a lista</a>
  </body>
</html>
